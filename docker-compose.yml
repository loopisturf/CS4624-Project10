version: '3.8'

services:
  backend:
    build:
      context: .  # This is the directory where the backend Dockerfile exists
      dockerfile: Dockerfile.backend
    ports:
      - "5000:5000"
    volumes:
      - ./energy_estimation.db:/app/energy_estimation.db
    env_file: .env
    depends_on:
      - frontend  # Ensure frontend is built before the backend

  frontend:
    build:
      context: ./front-end  # Path to your React app
      dockerfile: Dockerfile.frontend
    ports:
      - "3000:3000"
    command: npm run build  # Build the React app (don't serve it yet)